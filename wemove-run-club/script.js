const header=document.querySelector('header'),menu=document.querySelector('.menu');addEventListener('scroll',()=>header.classList.toggle('scrolled',scrollY>30),{passive:true});menu.addEventListener('click',()=>{const open=header.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});document.querySelectorAll('header nav a').forEach(a=>a.addEventListener('click',()=>header.classList.remove('open')));const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));document.querySelectorAll('[data-reason]').forEach(a=>a.addEventListener('click',()=>document.querySelector('[name="reason"]').value=a.dataset.reason));function wireForm(id,subjectPrefix){const form=document.getElementById(id);form.addEventListener('submit',event=>{event.preventDefault();const data=Object.fromEntries(new FormData(form));const subject=encodeURIComponent(`${subjectPrefix}: ${data.reason||data.firstName||data.name||'Website enquiry'}`);const body=encodeURIComponent(Object.entries(data).map(([key,value])=>`${key}: ${value||'Not supplied'}`).join('\n'));form.querySelector('.form-status').textContent='Your email app will open with the message prepared.';location.href=`mailto:wemoverunclub.admin@gmail.com?subject=${subject}&body=${body}`})}wireForm('join-form','WE MOVE join request');wireForm('contact-form','WE MOVE website enquiry');
