const menu=document.querySelector('.menu');const nav=document.querySelector('#navlinks');menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false')}));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));document.querySelector('#year').textContent=new Date().getFullYear();document.querySelector('#project-form').addEventListener('submit',e=>{e.preventDefault();const d=new FormData(e.currentTarget);const subject=encodeURIComponent(`Project enquiry — ${d.get('business')}`);const body=encodeURIComponent(`Hello Ascend Digital,\n\nFull name: ${d.get('name')}\nBusiness: ${d.get('business')}\nEmail: ${d.get('email')}\nPhone: ${d.get('phone')}\nService: ${d.get('service')}\nBudget: ${d.get('budget')}\n\nProject details:\n${d.get('details')}\n\nRegards,\n${d.get('name')}`);location.href=`mailto:ascenddigital.sa@gmail.com?subject=${subject}&body=${body}`});
