const tabs=document.querySelectorAll('[data-tab]');const panels=document.querySelectorAll('.tab');const title=document.querySelector('#title');tabs.forEach(tab=>tab.addEventListener('click',()=>{tabs.forEach(x=>x.classList.remove('active'));panels.forEach(x=>x.classList.remove('active'));tab.classList.add('active');document.getElementById(tab.dataset.tab).classList.add('active');title.textContent=tab.textContent.toUpperCase()}));const eventForm=document.querySelector('#event-form');const saved=localStorage.getItem('smghcEventDraft');if(saved){const data=JSON.parse(saved);Object.entries(data).forEach(([key,value])=>{if(eventForm.elements[key])eventForm.elements[key].value=value})}document.querySelectorAll('[data-save]').forEach(button=>button.addEventListener('click',()=>{localStorage.setItem('smghcEventDraft',JSON.stringify(Object.fromEntries(new FormData(eventForm))));document.querySelector('.saved').textContent='Draft saved in this browser only.'}));
